package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.OpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;

@TeleOp
public class TurretMechanism extends OpMode {
    @Override
    public void init() {
        //have them make the turret mechanism and build it so it's adjustable power
    }

    @Override
    public void loop() {
        //dylan use dcmotorextended so you can use the setvelocity function emma said its good
        //have them look at the limelight to see what functionality it could provide (importantly distance)
        //I forgot to but later i'll measure the field to see where the april tag is and make the limelight file for it
        //thanks ydlan :) 
    }
}
